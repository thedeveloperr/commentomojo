// import {
// } from "./actions";

const rootReducer = (state, action) => {
    let newState;
    switch (action.type) {
        default:
            return newState;
    }
};

export default rootReducer;